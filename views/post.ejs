<%- include('partials/header') %>

    <% posts.forEach(function(post) { %>
        <h2>
            <%= post.postTitle %>
        </h2>

        <p>
            <%= post.postBody %>
        </p>
        <% }); %>

            <%- include('partials/footer') %>